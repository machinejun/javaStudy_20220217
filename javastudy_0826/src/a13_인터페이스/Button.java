package a13_인터페이스;

public interface Button {
	
	public void onclick(); // 인터페이스의 메소드의 기본형태 = 무조건 전부다 abstract 메소드 형태
//	public default void onPressde() { 
//	/* default 를 붙여준다면 일반 메소드 형태가 된다. 하지만 인터페이스에서 일반메소드는 잘 사용하지 않는다. */
		
	}

